package filters;

import filters.text.FilterUppercase;
import model.Cell;

/*public class FilterFactory {

    public Filter getFilter(String filterType, Cell cell){
        if (filterType.equalsIgnoreCase(ConstFilters.UPPERCASE_FILTER)){
            return new FilterUppercase(cell);
        }
        else if (filterType.equalsIgnoreCase(ConstFilters.FORMULA_CHAR_FILTER)){
            return new FilterFormulaIntroChar(cell);
        }
        return null;
    }
}*/
